import { green, white, shadowTextBrownBig } from '../../../themes/variables';

const React = require('react-native');

const { StyleSheet } = React;

export default StyleSheet.create({
  header: {
    backgroundColor: green,
  },
  title: {
    color: white,
  },
  shadowTextBrownBig,
  iconMenu: {
    color: white,
    fontSize: 36,
    marginTop: 10,
  },
  iconArrow: {
    color: white,
  },
});
